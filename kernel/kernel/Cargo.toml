[package]
name = "kernel"
version = "0.1.0"
edition.workspace = true

[[bin]]
name = "kernel"
test = false
harness = false

[dependencies]
aarch64 = { path = "../aarch64" }
acpi = { path = "../acpi" }
boot-info = { path = "../boot-info" }
elf = { path = "../elf" }
kstd = { path = "../kstd" }
